# Continuous integration — lint, test, and publish the docs site.
#
# This workflow deliberately does NOT release. It used to also bump the version,
# push a tag and publish to PyPI, which made it self-triggering: the bump pushed
# a commit to master with a GitHub App token, app-token pushes DO start
# workflows, and that started another bump. It ran ~every 95 seconds and only
# stopped when a push happened to fail. Releasing is manual and lives in
# release.yml.
name: CI/CD Pipeline

on:
  push:
    branches: [ master ] # Or your default branch
  pull_request:
    branches: [ master ] # Or your default branch
  workflow_dispatch:

# Least privilege: nothing here writes to the repository. `pages`/`id-token` are
# required by actions/deploy-pages.
permissions:
  contents: read
  pages: write
  id-token: write


jobs:
  test-lint-type-build:
    runs-on: ubuntu-latest
    # Runs on pull requests as well as pushes. It previously carried
    # `if: github.ref == 'refs/heads/master'` to save Actions minutes, but that
    # meant a pull request got no checks at all — problems were only caught once
    # they were already on master, and the green badge described master rather
    # than the change being proposed.

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python "3.12"
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: |
        pip install uv
        uv venv .venv
        source .venv/bin/activate
        uv sync --all-groups
        uv pip install .

    - name: Lint with Ruff
      run: |
        uv run ruff check src tests

    # - name: Type check with Mypy
    #   run: |
    #     mypy src

    - name: Run tests with Pytest
      run: |
        uv run pytest tests

  build-docs:
    runs-on: ubuntu-latest
    needs: test-lint-type-build # Run only if tests pass
    # The docs are BUILT on every pull request, so a change that breaks Sphinx
    # fails the PR rather than the next push to master. Only the three Pages
    # steps below are gated on master — a pull request must never publish to the
    # live documentation site.

    steps:
        - name: Checkout
          uses: actions/checkout@v4
        
        - name: Set up Python
          uses: actions/setup-python@v4
          with:
            python-version: '3.12'
            
        - name: Build-docs
          run: |
            pip install uv
            uv venv .venv
            source .venv/bin/activate
            uv sync --all-groups
            uv pip install .
            cd docs && uv run sphinx-build -b html . _build/html
               
        # --- publish, master only -------------------------------------------
        # A pull request builds the docs above but must not deploy them.
        - name: Setup Pages
          if: github.ref == 'refs/heads/master'
          uses: actions/configure-pages@v5

        - name: Upload artifact
          if: github.ref == 'refs/heads/master'
          uses: actions/upload-pages-artifact@v3
          with:
            path: 'docs/_build/html'

        - name: Deploy to GitHub Pages
          if: github.ref == 'refs/heads/master'
          uses: actions/deploy-pages@v4
